#!/bin/bash

DB_TYPE="<%= @db_type %>"
APP_NAME="<%= @app_name %>"
BACKUP_EXT="<%= @backup_ext %>"
USER="<%= @user %>"
ACTION=$1

gem install fog -v 1.19.0

eybackup -c /root/aha_prod.backups.yml -e mysql --restore ${latest}

echo "Began database backup from production and restoring it to the clone database." >> /mnt/log/clone_database_restore.log