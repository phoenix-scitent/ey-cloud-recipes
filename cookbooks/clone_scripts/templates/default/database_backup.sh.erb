<!-- #!/bin/bash

# DB_TYPE="<%= @db_type %>"
# APP_NAME="<%= @app_name %>"
# BACKUP_EXT="<%= @backup_ext %>"
# USER="<%= @user %>"

# gem install fog -v 1.19.0

# latest=$(eybackup -c aha_prod.backups.yml -e mysql -l aha | tail -1 | cut -d ' ' -f 1)

echo "ran $latest succesfully and proceeding now to pulling production backup and restoring it" >> /mnt/log/clone_database_latest.log -->